server:
  port: 8081
  servlet:
    context-path: /springsecurity
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/security?characterEncoding=utf-8&serverTimezone=UTC
  redis:
    host: 43.138.84.4
    port: 6379
    connect-timeout: 5000ms
    lettuce:
      pool:
        max-active: 20
        max-idle: 10
        min-idle: 5
        max-wait: 5000ms
logging:
   level:
    com.tyut.mcf.security_study.mapper: debug